import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    mainLegendContainer: {
        flexDirection: "row",
        flexWrap: "wrap",
        alignItems: "center",
    },
    legendContainer: {
        flexDirection: "row",
        alignItems: "center",
        margin: 4,
    },
    legendColor: {
        width: 12,
        height: 12,
        marginRight: 6,
    },
})