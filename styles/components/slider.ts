import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    sliderContainer: {
        marginTop: 10,
    },
    sliderTitle: {
        fontWeight: "bold",
        marginBottom: 4,
        marginLeft: 16,
        fontSize: 16
    }
})